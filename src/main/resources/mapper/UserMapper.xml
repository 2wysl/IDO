<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="kopo.poly.persistance.mapper.IUserMapper">
    <!-- 회원 가입하기(회원정보 등록하기) -->
    <insert id="joinProc" parameterType="UserInfoDTO">
        INSERT INTO USER_INFO
        (USER_ID,
         PASSWORD,
         EMAIL,
         POST_CODE,
         ADDRESS,
         DT_ADDR)
        VALUES (#{user_id},
                #{password},
                #{email},
                #{post_code},
                #{address},
                #{dt_addr})
    </insert>

<!--    <select id="checkIdExists" parameterType="String" resultType="int">-->
<!--        SELECT COUNT(*)-->
<!--        FROM USER_INFO-->
<!--        WHERE USER_ID = #{userId}-->
<!--    </select>-->

    <select id="getLogin" parameterType="UserInfoDTO" resultType="UserInfoDTO">
        SELECT USER_ID, PASSWORD, EMAIL, POST_CODE, ADDRESS
        FROM USER_INFO
        WHERE USER_ID=#{user_id}
        AND PASSWORD=#{password}
    </select>



</mapper>